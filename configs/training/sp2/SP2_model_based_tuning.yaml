experiment_id: SP2_model_based_tuning_v1
mode: tune_model_based
output_dir: results/sp2/SP2_model_based_tuning_v1

tuning:
  output_artifact: outputs/tuning/SP2/model_based/v1/best_params.yaml
  method_param_grid:
    replicator_capacity:
      default:
        - {distance_weight: 0.28, deficit_weight: 1.0, reward_weight: 0.85, capacity_weight: 0.8, completion_weight: 0.0, exponent: 1.0}
        - {distance_weight: 0.18, deficit_weight: 1.2, reward_weight: 1.2, capacity_weight: 1.2, completion_weight: 0.0, exponent: 1.0}
        - {distance_weight: 0.12, deficit_weight: 1.2, reward_weight: 1.2, capacity_weight: 1.2, completion_weight: 1.5, exponent: 1.0}
    bnn_capacity:
      default:
        - {distance_weight: 0.22, deficit_weight: 1.15, reward_weight: 0.95, capacity_weight: 0.8, completion_weight: 0.0, exponent: 2.0}
        - {distance_weight: 0.14, deficit_weight: 1.3, reward_weight: 1.25, capacity_weight: 1.2, completion_weight: 0.0, exponent: 1.6}
        - {distance_weight: 0.1, deficit_weight: 1.3, reward_weight: 1.25, capacity_weight: 1.2, completion_weight: 1.5, exponent: 1.6}
    smith_capacity:
      default:
        - {distance_weight: 0.18, deficit_weight: 1.45, reward_weight: 1.0, capacity_weight: 1.2, completion_weight: 0.0, exponent: 1.15}
        - {distance_weight: 0.1, deficit_weight: 1.55, reward_weight: 1.35, capacity_weight: 1.45, completion_weight: 0.0, exponent: 1.0}
        - {distance_weight: 0.08, deficit_weight: 1.55, reward_weight: 1.35, capacity_weight: 1.45, completion_weight: 2.0, exponent: 1.0}
    primal_dual_capacity:
      default:
        - {distance_weight: 0.15, deficit_weight: 1.55, reward_weight: 1.1, capacity_weight: 1.35, completion_weight: 0.0}
        - {distance_weight: 0.08, deficit_weight: 1.55, reward_weight: 1.55, capacity_weight: 1.55, completion_weight: 0.0}
        - {distance_weight: 0.06, deficit_weight: 1.55, reward_weight: 1.55, capacity_weight: 1.55, completion_weight: 2.0}
    local_primal_dual_capacity:
      default:
        - {distance_weight: 0.14, deficit_weight: 1.55, reward_weight: 1.1, capacity_weight: 1.35, completion_weight: 0.0}
        - {distance_weight: 0.08, deficit_weight: 1.55, reward_weight: 1.55, capacity_weight: 1.55, completion_weight: 0.0}
        - {distance_weight: 0.04, deficit_weight: 1.55, reward_weight: 1.55, capacity_weight: 1.55, completion_weight: 2.0}

seeds:
  start: 1300
  count: 8

validation_seeds:
  start: 2300
  count: 6

scenarios:
  - param_generator: light_mixed
  - param_generator: balanced_capacity
  - param_generator: heavy_capacity
  - param_generator: battery_constrained
  - param_generator: monte_carlo

methods:
  - id: replicator_capacity
  - id: bnn_capacity
  - id: smith_capacity
  - id: primal_dual_capacity
  - id: local_primal_dual_capacity
