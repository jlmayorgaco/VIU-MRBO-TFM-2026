# Post-hoc claim audit

The computational campaign is complete and internally auditable: 960 base rows, 2,160 total rows, unique run IDs, zero numerical errors, immutable seed-registry hash restored, and a separate seed-opening event retained.

Two claim limits are mandatory:

1. `FULL_ROBUST_LOCAL` is a recovery proxy under degraded messages, not a fully decentralized local closure. `_replacement_robot` enumerates every idle robot and reads global capacity, health, and position arrays. No adjacency-constrained candidate set is enforced.
2. HOCBF does not establish unconditional safety. The obstacle/dropout family retains a 0.14 collision rate because projected safe accelerations are not always realizable by the selected wrench cone.

Supported claims are limited to the cumulative ladder inside the numerical model, the observed interaction between capacity, wrench and dynamic reserve, the bounded recovery proxy, and the common-Lyapunov ISS theorem under its fixed-load, no-reset and bounded-input assumptions.