# H3-B sensitivity not generated

The requested deadline/rate sensitivity sweep is marked as Phase 2 in the protocol, but no pre-existing artifact contains deadline-sweep data. Because this consolidation pass must not run new experiments, the script emits the existing paired H3-B result instead.
